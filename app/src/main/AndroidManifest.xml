<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bro.brocast" >

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
    I added cleartextraffic:true to the manifest. My guess is that it
    is because localhost testing is on http and it only allows https.
     TODO Don't forget to test and remove the cleartext for the final version
     I added the 'stateAlwaysHidden' to the messaging activity. This is to ignore the initial focus
     on the edit text field on that window. Any next call is also suppresed so that our own keyboard
     is the only one that the user can use.
     -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/brocast"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/brocast_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".OpeningActivity" android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".BroCastHome" />
        <activity
            android:name=".FindBroActivity"
            android:windowSoftInputMode="adjustNothing"/>
        <activity android:name=".MainActivity" android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".MessagingActivity"
            android:windowSoftInputMode="stateAlwaysHidden" />

        <service android:name=".notification.MessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>